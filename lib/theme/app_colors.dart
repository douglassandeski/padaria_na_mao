import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF1C1C1E);
  static const Color card = Color(0xFF2C2C2E);
  static const Color accent = Color(0xFF30D158); // verde inspirado no TradeMap
  static const Color income = Color(0xFF5AC8FA); // azul claro para receitas
  static const Color expense = Color(0xFFFF453A); // vermelho claro para despesas
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.grey;
}
